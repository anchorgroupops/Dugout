# This workflow is replaced by build-deploy.yml (GHCR + Watchtower).
# Kept as a no-op to avoid GitHub showing "removed workflow" warnings.
name: Deploy to Raspberry Pi (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-legacy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is replaced by build-deploy.yml. Use Watchtower for auto-deploy."
