# Core scraper — locked to match Playwright base image
playwright==1.62.0

# Web framework + WSGI server
flask>=3.1.3,<4
flask-cors>=6.0.5,<7
gunicorn>=26.0.0,<27

# HTTP client (security-sensitive: CVEs in <2.31)
requests>=2.34.2,<3

# PDF parsing + image handling for scorebook OCR
pdfplumber>=0.11.10,<1
Pillow>=12.3.0,<13

# AI / vector APIs
google-generativeai>=0.8.6,<1
pinecone>=9.1.0,<10

# FastAPI (used by modal_app.py)
fastapi[standard]>=0.141.1,<1
uvicorn>=0.52.1,<1

# Utilities
python-dotenv>=1.2.2,<2
pyotp>=2.10.0,<3
tzdata>=2026.3

# GC CSV autopull — Gmail OAuth (2FA code read + notification send)
google-api-python-client>=2.198.0
google-auth>=2.56.2
google-auth-oauthlib>=1.4.0
google-auth-httplib2>=0.4.1

# GC CSV autopull — LLM-adaptive locator fallback
anthropic>=0.122.0

# Multi-team registry
PyYAML>=6.0.3,<7

# Walk-up song search + download (YouTube via yt-dlp, requires ffmpeg for MP3 extraction)
yt-dlp>=2026.7.4

# TTS providers — free tier chain (install whichever are available on the host)
# Edge TTS: Microsoft neural voices, no API key, works on Pi
edge-tts>=7.2.8
# Kokoro ONNX: local 82M-param model, MIT license, ARM64-compatible, ~300MB download on first use
# pip install kokoro-onnx   (optional — not pinned here, install manually on Pi/PC)
# Google Cloud TTS: free tier 1M Neural2 chars/month
# pip install google-cloud-texttospeech  (optional — needs GOOGLE_APPLICATION_CREDENTIALS)
